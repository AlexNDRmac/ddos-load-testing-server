name: Benchmark

on:
  push:
    paths-ignore:
      - '**.md'
  pull_request:
    branches:
      - 'master'

jobs:
  bench:
    name: Bench ${{ matrix.name }}

    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
      matrix:
        tool: ['dripper']

        name:
          - 'DRipper'

        include:
          - { name: 'DRipper', image: 'alexmon1989/dripper:latest' }

    steps:
      - uses: actions/checkout@v2

      - name: Cache node modules
        uses: actions/cache@v2
        env:
          cache-name: cache-node-modules
        with:
          path: ./node_modules
          key: ${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.os }}-build-
            ${{ runner.os }}-

      - name: Install Dependencies
        run: |
          npm install

      - name: Start test server
        run: |
          node app -p 8070 &
          sleep 5
          curl --location http://localhost:8070/start/http/5555

      - name: Run benchmark
        run: |
          docker run -i --rm ${{ matrix.image }} -t 100 -s http://localhost:5555 -d 60

      - name: Get benchmark result
        run: |
          curl --location http://localhost:8070/stop




